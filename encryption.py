import os
import boto3
import base58
import base64
from dotenv import load_dotenv
load_dotenv('.env')

# Add your encrypt/decrypt details here OMMITTED for security

def encrypt_private_key(private_key: str) -> str:
    # Add your encrypt details here OMMITTED for security

def decrypt_private_key(encrypted_private_key: str) -> str:
    # Add your decrypt details here OMMITTED for security
